jQuery(document).ready(function($) {
	// Comment
	GetComments.init();
	CreateComment.init();
	DeactiveComment.init();

	// Bookmark
	CreateUp.init();
	DeleteUp.init();

	// Post
	DeactivePost.init();
});
